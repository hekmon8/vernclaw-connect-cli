# 2026-04-05 CLI Release 0.1.5

## 目标

发布 `vernclaw-connect-cli@0.1.5`，同时完成 npm 发布与 GitHub tag/release。

## 范围

- 版本从 `0.1.4` 升到 `0.1.5`
- 包含 README 与 skills 文档同步
- 不包含额外功能改动

## 发布步骤

1. 更新 `package.json` 版本到 `0.1.5`
2. 运行构建与测试验证
3. 执行 `npm publish`
4. 创建 Git tag `v0.1.5` 并推送
5. 创建 GitHub Release

## 验证

- `npm run build`
- `npm test`
- `npm pack --dry-run`
